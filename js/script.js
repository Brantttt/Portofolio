const BuscaProjetos = document.querySelector("#BuscaProjetos");

let projetos = [];

BuscaProjetos.addEventListener("input", function(){
    const texto = BuscaProjetos.ariaValueMax.toLowerCase();

    const filtrados = cursos.filter((projetos) =>

    )
    
});